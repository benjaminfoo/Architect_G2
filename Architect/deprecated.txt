SpawnConstructionWithPosition

// TODO!
// TODO! 
// TODO: 
// - identify spawned objects by some parameter, like interactive, useable, or something else, ...

// TODO 
// if(useable) {
		 // set focus name
//     SetMobName(vobPointer, "MOBNAME_CHEST");            
// }

// if(lock / chest) { // TODO: define and determine proper parameter
// // A key and/or lock picking string can be set	
// LockMobLockable(vobPointer,  "ItKe_Key_01", "LRLR", TRUE);

// Whoops, dropped the key inside!
// This way, a container could get setup 
// FillMobContainer(vobPointer, "ItMi_Gold:23,ItKe_Key_01:1 ");
// }